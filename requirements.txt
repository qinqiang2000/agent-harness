# FastAPI and web framework dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sse-starlette>=1.8.0
pydantic>=2.0.0
python-dotenv>=1.0.0
aiohttp>=3.9.0             # Async HTTP client (for Yunzhijia webhook)
httpx[socks]>=0.27.0      # HTTP client with SOCKS proxy support (for Zhichi)

# Claude Agent SDK
anyio>=4.0.0
claude-agent-sdk
anthropic>=0.40.0            # LLM judge for score_results.py and analyze_bad_cases.py

# Database (for context data access if needed)
pymysql>=1.0.0
asyncpg>=0.29.0            # PostgreSQL async driver for operational-analytics skill
aiomysql>=0.2.0            # MySQL async driver for issue-diagnosis db queries
psycopg2-binary>=2.9.11    # PostgreSQL sync driver (fallback)
tqdm
tabulate>=0.9.0            # Data formatting for operational-analytics

# KDUBL XML validator dependencies
lxml>=5.0.0               # XML parsing, XSD and XSLT 1.0 validation
saxonche>=12.0.0          # Saxon-HE Python: XSLT 2.0/3.0 support (optional)

# CLI tool dependencies
prompt-toolkit>=3.0.43    # Interactive REPL
rich>=13.7.0              # Terminal formatting

# Audit Plugin PDF Preview
PyMuPDF>=1.24.0
rapidocr-onnxruntime>=1.4.0  # OCR fallback for image-based PDFs
apscheduler>=3.10.0
